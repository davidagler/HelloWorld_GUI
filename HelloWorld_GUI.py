from tkinter import *

root = Tk() #root is blank window
label = Label(root, text="Hello World.") #root is blank window
label.pack() #just pack that text in there - so lazy
root.mainloop() #display it it in an infinite loop
